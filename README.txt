TASK No.2
=========

Assumptions:
1. The webserver has been configured correctly to direct the URL specify below to getinfo.php.
2. The Mysql Server has been configured and running.

Please raise at least 10 suggestions/corrections about these codes.


SOLUTION
========
a) I assume getinfo.php configured to run without use of mod_rewrite. 
b) I assume provaded data.sql structure dump designed for MySQL DB.
c) I assume MySQL DB will use UTF-8 encoding because in the data.sql not specified.
d) All suggestions/corrections are inside the code itself (in the inline comments).
e) Source code files with suggestions/corrections are located in the '\src' folder.